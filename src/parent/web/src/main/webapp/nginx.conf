
user  root;
worker_processes  1;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    include /etc/nginx/conf.d/*.conf;

#add my  https server config
	
	upstream tomcat_group {
          #  ip_hash;   #client ip hash allot server fix session problem
	 #   server 127.0.0.1:8080;    
   	    server 127.0.0.1:8088;    
        }
	upstream webSSH{
 		server 127.0.0.1:1443;
	}




#webSSH server
server {

        listen       443  ssl;
        server_name ssh.tenie.net;
        #ssl on;
        #ssl_certificate /root/resumeSSL/Nginx/1_resume.tenie.net_bundle.crt;
        #ssl_certificate_key /root/resumeSSL/Nginx/2_resume.tenie.net.key;
        client_max_body_size 1024M;


        location /{
            proxy_pass https://webSSH;
           # root /usr/local/tomcat/apache-tomcat-9.0.0.M18/webapps/ROOT;
            #index resumepage.html;
       }

    }



#resume
server {

        listen       443  ssl;
        server_name resume.tenie.net;
        ssl on;
        ssl_certificate /root/resumeSSL/Nginx/1_resume.tenie.net_bundle.crt;
        ssl_certificate_key /root/resumeSSL/Nginx/2_resume.tenie.net.key;
        client_max_body_size 1024M;
       

	location /{
            proxy_pass http://tomcat_group;
           # root /usr/local/tomcat/apache-tomcat-9.0.0.M18/webapps/ROOT; 
  	    #index resumepage.html;
       }
#when access site  resume.tenie.net point to  resumepage.html 
	location =/{
            proxy_pass http://tomcat_group/resumepage.html;
           # root /usr/local/tomcat/apache-tomcat-9.0.0.M18/webapps/ROOT;
            #index resumepage.html;
       }

    }

#default server 
	server {  
        
	listen       443  ssl default_server;                                                        
	#server_name ~^(www\.)?(.+)$;                                              
        ssl on;
#       ssl_certificate /root/Nginx/1_www.tenie.net_bundle.crt;
#   	ssl_certificate_key /root/Nginx/2_www.tenie.net.key;
	
	ssl_certificate /root/myCertificate/www_tenie_net_2020/Nginx/1_www.tenie.net_bundle.crt;
	ssl_certificate_key /root/myCertificate/www_tenie_net_2020/Nginx/2_www.tenie.net.key;

	client_max_body_size 1024M;
       
  #	if ($host = resume.tenie.net){
   #      rewrite ^ https://resume.tenie.net/resumepage.html  permanent;
   #  }  


   # location ~ .*\.(html|htm|gif|jpg|jpeg|bmp|png|ico|txt|js|css)$ 
   # {
   #     root /usr/local/tomcat/apache-tomcat-9.0.0.M182/webapps/ROOT;
   #  #   expires      7d; 
   #  } 	



#webSSH
        location /webSSH {
             proxy_pass https://webSSH;

            #set client real info submit to server;
            # proxy_set_header Host $host:$server_port;
            # proxy_set_header Host $remote_addr:$remote_port;
            # proxy_set_header X-Real-IP $remote_addr;
            # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           # root /home/tenie/Theme;
            index http://tomcat_group/index.html;
       }
# bt client -- transmission
#        location /transmission {
#             proxy_pass http://127.0.0.1:9091/transmission;

            #set client real info submit to server;
            # proxy_set_header Host $host:$server_port;
            # proxy_set_header Host $remote_addr:$remote_port;
            # proxy_set_header X-Real-IP $remote_addr;
            # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           # root /home/tenie/Theme;
#            index http://127.0.0.1:9091/transmission/web;
#       }


# my toshiba pc bt client  transmission
        location /transmission {
             proxy_pass http://127.0.0.1:22021/transmission;

            #set client real info submit to server;
            # proxy_set_header Host $host:$server_port;
            # proxy_set_header Host $remote_addr:$remote_port;
            # proxy_set_header X-Real-IP $remote_addr;
            # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
             # root /home/tenie/Theme;
 # set timeout
            autoindex_exact_size off; 
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header   Host $host;
            client_max_body_size     100m;
            client_body_buffer_size  128k;
            proxy_connect_timeout    1200;
            proxy_read_timeout       1200;
            proxy_send_timeout       6000;
            proxy_buffer_size        32k;
            proxy_buffers            4 64k;
            proxy_busy_buffers_size 128k;
            proxy_temp_file_write_size 128k;
            autoindex on;
            limit_rate_after 15m; ## 达到最大带宽时开始限制
            limit_rate 15m; ## 带宽限制

            index http://127.0.0.1:22021/transmission/web;
       }

# my toshiba pc nextCloud
        location /nextcloud {
             proxy_pass http://127.0.0.1:22022/nextcloud;

            #set client real info submit to server;
            # proxy_set_header Host $host:$server_port;
            # proxy_set_header Host $remote_addr:$remote_port;
            # proxy_set_header X-Real-IP $remote_addr;
            # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # root /home/tenie/Theme;
	   
 # set timeout 
	    autoindex_exact_size off;         
            proxy_set_header X-Real-IP $remote_addr;  
            proxy_set_header   Host $host;  
            client_max_body_size     100m;  
            client_body_buffer_size  128k;  
            proxy_connect_timeout    1200;  
            proxy_read_timeout       1200;  
            proxy_send_timeout       6000;  
            proxy_buffer_size        32k;  
            proxy_buffers            4 64k; 
            proxy_busy_buffers_size 128k;  
            proxy_temp_file_write_size 128k;  
            autoindex on; 
            limit_rate_after 15m; ## 达到最大带宽时开始限制  
            limit_rate 15m; ## 带宽限制
	

            index http://127.0.0.1:22022/nextcloud/index.php;
       }






        location / {
             proxy_pass http://tomcat_group;

	    #set client real info submit to server;
            # proxy_set_header Host $host:$server_port;
             proxy_set_header Host $remote_addr:$remote_port;
	     proxy_set_header X-Real-IP $remote_addr;
             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       	   # root /home/tenie/Theme;
	    index http://tomcat_group/index.html;  
             
	    location ~ .*\.(html|htm|gif|jpg|jpeg|bmp|png|ico|txt|js|css)$
            {
               root /usr/local/tomcat/apache-tomcat-9.0.0.M182/webapps/ROOT;
               # expires      7d;
            }


       }
    }

	server {  
    		listen  80;  
    		server_name ~^(www\.)?(.+)$;  
      	           
              #  location / {
	      #	   proxy_pass https://www.tenie.net;
              #		}
	        rewrite ^(.*)$  https://$host$1 permanent;  
	      #  rewrite ^(.*)$  https://www.tenie.net$1 permanent;  
	}  

	
}
